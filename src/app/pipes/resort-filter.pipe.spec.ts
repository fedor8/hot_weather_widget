import { ResortFilterPipe } from './resort-filter.pipe';

describe('ResortFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResortFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
